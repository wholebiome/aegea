production:
  adapter: postgresql
  encoding: utf8
  database: arvados_sso_production
  username: arvados_sso
  password: $ARVADOS_SSO_PRODUCTION_DB_PASSWORD
  host: $ARVADOS_SSO_PRODUCTION_DB_HOST
  template: template0

development:
  adapter: postgresql
  encoding: utf8
  database: arvados_sso_development
  username: arvados_sso
  password: $ARVADOS_SSO_DEVELOPMENT_DB_PASSWORD
  host: $ARVADOS_SSO_DEVELOPMENT_DB_HOST
  template: template0

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  database: arvados_sso_test
  username: arvados_sso
  password: $ARVADOS_SSO_TEST_DB_PASSWORD
  host: $ARVADOS_SSO_TEST_DB_HOST
  template: template0
