test:
  override:
    - tox
  post:
    - bash <(curl -s https://codecov.io/bash) -t a57887f4-abd7-434a-abad-370d85438174

dependencies:
  override:
    - pip install tox tox-pyenv
    - pyenv local 2.7.10 3.5.0
